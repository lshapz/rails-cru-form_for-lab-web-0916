<%= form_for(@artist) do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: 'enter artist name' %>
  <%= f.label :bio %>
  <%= f.text_field :bio, placeholder: 'artist bio' %>    
  <%= f.submit%>

<% end %>