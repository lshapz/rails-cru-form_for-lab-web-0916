<%= form_for(@song) do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name, placeholder: 'enter song name' %>
  <%= f.text_field :artist_id, placeholder: 'enter artist_id' %>
    <%= f.text_field :genre_id, placeholder: 'enter genre_id' %>

  <%= f.submit%>

<% end %>